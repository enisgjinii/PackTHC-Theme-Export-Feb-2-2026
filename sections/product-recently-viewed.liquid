{%- liquid
	assign section_id   = '#shopify-section-' | append: section.id
	assign full         = section.settings.full
	assign layout       = section.settings.layout
	assign columns      = section.settings.columns
	assign total        = section.settings.rows | times: columns
	assign show_arrows  = section.settings.show_arrows
	assign heading_size = section.settings.heading_size
	assign show_dots    = section.settings.show_dots
	assign column_gap   = 30
-%}

<script src="{{ 'product-recently-viewed.js' | asset_url }}" defer="defer"></script>

<style>
	{{ section_id }}.has-products-recently .section-inner {
		padding-top: {{ section.settings.padding_top_mobile }}px;
		padding-bottom: {{ section.settings.padding_bottom_mobile }}px;
	}

	@media (min-width: 992px) {
		{{ section_id }}.has-products-recently .section-inner {
			padding-top: {{ section.settings.padding_top }}px;
			padding-bottom: {{ section.settings.padding_bottom }}px;
		}

		{{ section_id }} .slider-layout .swiper:not(.swiper-initialized) .swiper-slide {
			width: calc( ( 100% - {{ columns | minus: 1 | times: column_gap }}px ) / {{ columns }} );
			margin-right: {{ column_gap }}px;
		}
	}

	@media (max-width: 991px) {
		{{ section_id }} .swiper:not(.swiper-initialized) .swiper-slide {
			margin-right: {{ column_gap }}px;
		}
	}
</style>

{%- if collections.all.products.size > 0 -%}
	<div class="section-inner slider-layout color-{{ section.settings.color_scheme }}">
		<div class="{% if full %}stretch-section stretch-padding{% else %}container{% endif %}">
			<div class="product-recently-viewed" data-url="/search?section_id={{ section.id }}&type=product&q=">
				{%- if search.performed and search.results.size > 0 -%}
					{%- if section.settings.heading != blank -%}
						<div class="wrapper-heading">
							<h2 class="title {{ heading_size }} rte">{{- section.settings.heading | escape -}}</h2>
						</div>
					{%- endif -%}

					{%- if layout == 'slider' -%}
						<div class="carousel-swiper">
							<template data-options data-pagination-mobile data-css-mode="off">
								{
									{%- if show_arrows -%}
										"navigation": {
											"nextEl": "{{ section_id }} .swiper-button-next",
											"prevEl": "{{ section_id }} .swiper-button-prev"
										},
									{%- endif -%}

									{%- if show_dots -%}
										"pagination": {
											"el": "{{ section_id }} .swiper-pagination",
											"clickable": true
										},
									{%- endif -%}

									"spaceBetween": 30,
									"slidesPerView": {{ columns }},
									"breakpoints": {
										"240": {
											"slidesPerView": 1.3,
											"spaceBetween": 15
										},
										"992": {
											"slidesPerView": 4
										}
									}
								}
							</template>

							<div class="swiper">
					{%- endif -%}

					<div class="{% if layout == 'grid' %}grid grid-{{ columns }} grid-tablet-3 grid-mobile-2{% else %}swiper-wrapper{% endif %}">
						{%- for product in search.results limit: total -%}
							<div class="{% if layout == 'grid' %}grid-item{% else %}swiper-slide{% endif %}">
								{%- render 'product-card', product: product, index: forloop.index -%}
							</div>
						{%- endfor -%}
					</div>

					{%- if layout == 'slider' -%}
						</div>{% comment %} .swiper {% endcomment %}

						{%- if layout == 'slider' and show_dots or show_arrows -%}
							<div class="swiper-controls">
								{%- if show_arrows -%}
									<button class="swiper-button-prev" title ="{{ 'general.swipper.prev' | t }}" aria-label="{{ 'general.swipper.prev' | t }}" id="{{ 'general.swipper.prev' | t }}" aria-labelledby="{{ 'general.swipper.prev' | t }}">{%- render 'icon', code: 'arrow-left-slider' -%}</button>
								{%- endif -%}

								{%- if show_dots -%}
									<div class="swiper-pagination" tabindex="-1"></div>
								{%- endif -%}

								{%- if show_arrows -%}
									<button class="swiper-button-next" title ="{{ 'general.swipper.next' | t }}" aria-label="{{ 'general.swipper.next' | t }}" id="{{ 'general.swipper.next' | t }}" aria-labelledby="{{ 'general.swipper.next' | t }}">{%- render 'icon', code: 'arrow-right-slider' -%}</button>
								{%- endif -%}
							</div>
						{%- endif -%}

						</div>{% comment %} .slider-swiper {% endcomment %}
					{%- endif -%}
				{%- endif -%}
			</div>
		</div>
	</div>
{%- endif -%}

{% schema %}
{
	"name": "t:sections.product-recently-viewed.name",
	"tag": "section",
	"class": "recently-viewed-section",
	"limit": 1,
	"disabled_on": {
		"groups": ["header", "footer"]
	},
	"settings": [
		{
			"type": "paragraph",
			"content": "t:sections.product-recently-viewed.settings.paragraph_1.content"
		},
		{
			"type": "checkbox",
			"id": "full",
			"default": false,
			"label": "t:general.layout.full.label"
		},
		{
			"type": "color_scheme",
			"id": "color_scheme",
			"label": "t:settings_schema.colors.label",
			"default": "scheme-1"
		},
		{
			"type": "inline_richtext",
			"id": "heading",
			"default": "You may also like",
			"label": "t:general.typography.heading.label"
		},
		{
			"type": "select",
			"id": "heading_size",
			"options": [
				{
					"value": "h4",
					"label": "t:general.typography.heading_size.option_1.label"
				},
				{
					"value": "h3",
					"label": "t:general.typography.heading_size.option_2.label"
				},
				{
					"value": "h2",
					"label": "t:general.typography.heading_size.option_3.label"
				}
			],
			"default": "h2",
			"label": "t:general.typography.heading_size.label"
		},
		{
			"type": "select",
			"id": "layout",
			"options": [
				{
					"value": "grid",
					"label": "t:sections.product-recently-viewed.settings.layout.options__1.label"
				},
				{
					"value": "slider",
					"label": "t:sections.product-recently-viewed.settings.layout.options__2.label"
				}
			],
			"default": "grid",
			"label": "t:sections.product-recently-viewed.settings.layout.label"
		},
		{
			"type": "range",
			"id": "rows",
			"min": 1,
			"max": 10,
			"step": 1,
			"default": 1,
			"label": "t:sections.product-recently-viewed.settings.rows.label"
		},
		{
			"type": "range",
			"id": "columns",
			"min": 1,
			"max": 12,
			"step": 1,
			"default": 4,
			"label": "t:sections.product-recently-viewed.settings.columns.label"
		},
		{
			"type": "header",
			"content": "t:general.slider.options.label"
		},
		{
			"type": "checkbox",
			"id": "show_arrows",
			"default": true,
			"label": "t:general.slider.show_arrows.label"
		},
		{
			"type": "checkbox",
			"id": "show_dots",
			"default": true,
			"label": "t:general.slider.show_dots.label"
		},
		{
			"type": "header",
			"content": "t:general.padding.header_padding.content"
		},
		{
			"type": "range",
			"id": "padding_top",
			"min": 0,
			"max": 100,
			"step": 1,
			"default": 0,
			"unit": "px",
			"label": "t:general.padding.top.label"
		},
		{
			"type": "range",
			"id": "padding_bottom",
			"min": 0,
			"max": 100,
			"step": 1,
			"default": 50,
			"unit": "px",
			"label": "t:general.padding.bottom.label"
		},
		{
			"type": "paragraph",
			"content": "t:general.padding.paragraph_mobile.content"
		},
		{
			"type": "range",
			"id": "padding_top_mobile",
			"min": 0,
			"max": 100,
			"step": 1,
			"default": 20,
			"unit": "px",
			"label": "t:general.padding.top.label"
		},
		{
			"type": "range",
			"id": "padding_bottom_mobile",
			"min": 0,
			"max": 100,
			"step": 1,
			"default": 20,
			"unit": "px",
			"label": "t:general.padding.bottom.label"
		}
	],
	"presets": [
		{
			"name": "t:sections.product-recently-viewed.name"
		}
	]
}
{% endschema %}
